using System.Collections;
using System.Collections.Generic;
using UnityEngine;

enum active
{
    OFF = 0,
    ON = 1
}

public class HandWave : MonoBehaviour
{
    private active currentState = active.ON;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
